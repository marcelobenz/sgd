<div class="iso-panel mb-4"><div class="iso-panel-header"><h2 class="iso-section-title mb-0">Objetivos de calidad</h2></div><div class="table-responsive"><table class="table iso-table mb-0"><thead><tr><th>Objetivo</th><th>Indicador y meta</th><th>Resultado</th><th>Cumplimiento</th><th>Evaluaci&oacute;n de cierre</th></tr></thead><tbody>@forelse($objetivos as $objetivo)@php($indicador=$objetivo->indicadorPrincipal)@php($cierre=$objetivo->evaluaciones->firstWhere('tipo','cierre_periodo'))<tr><td><a href="{{ route('planificacion.objetivos.show',$objetivo) }}"><strong>{{ $objetivo->codigo }} &mdash; {{ $objetivo->titulo }}</strong></a><div>{{ $objetivo->proceso }}</div></td><td>{{ $indicador?->nombre ?? 'Sin indicador' }}<div>Meta: {{ $indicador?->meta }} {{ $indicador?->unidad }}</div></td><td>{{ $objetivo->resultado_actual ?? 'Sin mediciones' }} @if($objetivo->resultado_actual!==null){{ $indicador?->unidad }}@endif</td><td>{{ ucfirst($objetivo->cumplimiento_actual) }}<div>{{ ucfirst($objetivo->estado) }}</div></td><td>@if($cierre)<span class="report-status ok">Evaluado {{ $cierre->fecha_evaluacion->format('d/m/Y') }}</span><div>{{ ucfirst(str_replace('_',' ',$cierre->decision)) }}</div>@else<span class="report-status warn">Pendiente</span>@endif</td></tr>@empty<tr><td colspan="5">Sin objetivos de calidad.</td></tr>@endforelse</tbody></table></div></div>
